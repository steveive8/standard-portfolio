import React from 'react';
import {Flex, Col, Row, Text} from './common/plain';

export const Footer = ({}) => {
    return (
        <Flex>
            
        </Flex>
    )
}

export default Footer;